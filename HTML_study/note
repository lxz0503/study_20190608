#<!DOCTYPE> 声明不是一个 HTML 标签；它是用来告知 Web 浏览器页面使用了哪种 HTML 版本

#<!--  ... --> 注释标签用来在源文档中插入注释。注释不会在浏览器中显示

#<a> 标签定义超链接，用于从一个页面链接到另一个页面。
##<a> 元素最重要的属性是 href 属性，它指定链接的目标.
##提示：如果没有使用 href 属性，则不能使用 hreflang、media、rel、target 以及 type 属性。
##target属性可以用来指定如何跳转到其他链接
_blank	在新窗口中打开被链接文档


#<abbr> 标签用来表示一个缩写词或者首字母缩略词，如"WWW"或者"NATO"。
##提示：在某些浏览器中，当您把鼠标移至带有 <abbr> 标签的缩写词/首字母缩略词上时，<abbr> 标签的 title 属性可被用来展示缩写词/首字母缩略词的完整版本。

#<address> 标签定义文档作者/所有者的联系信息

#<area> 标签定义图像映射内部的区域（图像映射指的是带有可点击区域的图像）。
##<area> 元素始终嵌套在 <map> 标签内部。

#<article> 标签定义独立的内容。 HTML5 的新标签
##<article> 标签定义的内容本身必须是有意义的且必须是独立于文档的其余部分。

#<aside> 标签定义 <article> 标签外的内容。
##aside 的内容应该与附近的内容相关,提示：<aside> 的内容可用作文章的侧栏

#<audio> 标签定义声音，比如音乐或其他音频流。 标签是 HTML5 的新标签
##目前，<audio> 元素支持的3种文件格式：MP3、Wav、Ogg。
##属性包括controls,loop,autoplay等

#<b> 提示：您也可以使用 CSS 的 "font-weight" 属性设置粗体文本
#<base> 标签为页面上的所有的相对链接规定默认 URL 或默认目标。

在一个文档中，最多能使用一个 <base> 元素。<base> 标签必须位于 <head> 元素内部。

提示和注释
提示：请把 <base> 标签排在 <head> 元素中第一个元素的位置，这样 head 区域中其他元素就可以使用 <base> 元素中的信息了。

注释：如果使用了 <base> 标签，则必须具备 href 属性或者 target 属性或者两个属性都具备。

#bdi 指的是 bidi 隔离（Bi-directional Isolation）。 是 HTML5 的新标签

<bdi> 标签允许您设置一段文本，使其脱离其父元素的文本方向设置。

在发布用户评论或其他您无法完全控制的内容时，该标签很有用
#HTML5 不支持 <big> 标签，HTML 4.01 支持 <big> 标签。用CSS代替

#<blockquote> 引用其他来源
#<br> 标签插入一个简单的换行符。

<br> 标签是一个空标签，意味着它没有结束标签
#<button>
<button> 标签定义一个按钮。

在 <button> 元素内部，您可以放置内容，比如文本或图像。这是该元素与使用 <input> 元素创建的按钮之间的不同之处。

提示：请始终为 <button> 元素规定 type 属性。不同的浏览器对 <button> 元素的 type 属性使用不同的默认值。

#<canvas> 标签是 HTML5 中的新标签。
# <caption> 标签定义表格的标题。
<caption> 标签必须直接放置到 <table> 标签之后。
您只能对每个表格定义一个标题。
提示：通常这个标题会被居中于表格之上
# HTML5 不支持 <center> 标签。请用 CSS 代替
# 在 HTML5 中，<cite> 标签定义作品的标题。

在 HTML 4.01 中，<cite> 标签定义一个引用
# <colgroup> 标签用于对表格中的列进行组合，以便对其进行格式化。

通过使用 <colgroup> 标签，可以向整个列应用样式，而不需要重复为每个单元格或每一行设置样式。

注释：只能在 <table> 元素之内，在任何一个 <caption> 元素之后，在任何一个 <thead>、<tbody>、<tfoot>、<tr> 元素之前使用 <colgroup> 标签。

提示：如果想对 <colgroup> 中的某列定义不同的属性，请在 <colgroup> 标签内使用 <col> 标签

#<datalist> 标签规定了 <input> 元素可能的选项列表。

<datalist> 标签被用来在为 <input> 元素提供"自动完成"的特性。用户能看到一个下拉列表，里边的选项是预先定义好的，将作为用户的输入数据。

请使用 <input> 元素的 list 属性来绑定 <datalist> 元素
# <dd> 标签被用来对一个描述列表中的项目/名字进行描述。

<dd> 标签与 <dl> （定义一个描述列表）和 <dt> （定义项目/名字）一起使用。

在 <dd> 标签内，您能放置段落、换行、图片、链接、列表等等
# <dialog> 标签是 HTML5 中的新标签  <dialog> 标签定义一个对话框、确认框或窗口
# <div> 标签定义 HTML 文档中的一个分隔区块或者一个区域部分。

# <div>标签常用于组合块级元素，以便通过 CSS 来对这些元素进行格式化。

提示和注释
提示：<div> 元素经常与 CSS 一起使用，用来布局网页。

注释：默认情况下，浏览器通常会在 <div> 元素前后放置一个换行符。然而，您可以通过使用 CSS 改变这种情况
# <embed> 标签定义了一个容器，用来嵌入外部应用或者互动程序（插件）
# <fieldset> 标签可以将表单内的相关元素分组。
<fieldset> 标签会在相关表单元素周围绘制边框
<legend> 标签为 <fieldset> 元素定义标题
# <figcaption> 标签为 <figure> 元素定义标题。<figcaption> 标签是 HTML5 中的新标签
<figcaption> 元素应该被置于 <figure> 元素的第一个或最后一个子元素的位置
使用 <figure> 元素标记文档中的一个图像。<figure> 元素带有一个标题
# 使用 <figure> 元素标记文档中的一个图像：
# <footer> 标签是 HTML 5 中的新标签
<footer> 标签定义文档或者文档的一部分区域的页脚。
<footer> 元素应该包含它所包含的元素的信息。
在典型情况下，该元素会包含文档创作者的姓名、文档的版权信息、使用条款的链接、联系信息等等。
在一个文档中，您可以定义多个 <footer> 元素
# form 
https://baike.baidu.com/item/HTML%E8%A1%A8%E5%8D%95/11046337?fr=aladdin
<form> 标签用于创建供用户输入的 HTML 表单。需要前后端交互的时候就要form

<form> 元素包含一个或多个如下的表单元素：

<input>
<textarea>
<button>
<select>
<option>
<optgroup>
<fieldset>
<label>
# hr <hr> 标签定义 HTML 页面中的主题变化（比如话题的转移），并显示为一条水平线。

<hr> 元素被用来分隔 HTML 页面中的内容（或者定义一个变化）。
# <i> 定义与文本中其余部分不同的部分，并把这部分文本呈现为斜体文本
# <img> 标签定义 HTML 页面中的图像。
<img> 标签有两个必需的属性：src 和 alt
alt 与 title 属性区别：
图片中的 alt 属性是在图片不能正常显示时出现的文本提示。
图片中的 title 属性是在鼠标在移动到元素上的文本提示

通过在 <a> 标签中嵌套 <img> 标签，给图像添加到另一个文档的链接
# <input> 标签规定了用户可以在其中输入数据的输入字段。

<input> 元素在 <form> 元素中使用，用来声明允许用户输入数据的 input 控件。
在 HTML 中，<input> 标签没有结束标签。
在 XHTML 中，<input> 标签必须被正确地关闭

输入字段可通过多种方式改变，取决于 type 属性。
# <ins> 标签定义已经被插入文档中的文本。
# <label> 标签为 input 元素定义标注标记,和input元素成对出现
# <li> 标签定义列表项目
# <link> 标签最常见的用途是链接样式表。

注意： link 元素是空元素，它仅包含属性。

注意： 此元素只能存在于 head 部分，不过它可出现任何次数。
# mark<mark> 标签定义带有记号的文本。

请在需要突出显示文本时使用 <mark> 标签

# <meta> 标签提供了 HTML 文档的元数据。元数据不会显示在客户端，但是会被浏览器解析，不是给客户看的，是给浏览器用的
实例 1 - 定义文档关键词，用于搜索引擎：也就是搜索content里面的关键字就能关联到这个网址
<meta name="keywords" content="HTML, CSS, XML, XHTML, JavaScript">

实例 2 - 定义web页面描述：
<meta name="description" content="Free Web tutorials on HTML and CSS">

实例 3 - 定义页面作者：
<meta name="author" content="Hege Refsnes">

实例 4 - 每30秒刷新页面：
<meta http-equiv="refresh" content="30">

# <nav> 标签定义导航链接的部分   是 HTML5 的新标签
# <option> 标签定义下拉列表中的一个选项（一个条目）。

<option> 标签中的内容作为 <select> 或者<datalist> 一个元素使用
<option> 标签可以在不带有任何属性的情况下使用，但是您通常需要使用 value 属性，此属性会指示出被送往服务器的内容。

注释：请与 select 元素配合使用此标签，否则这个标签是没有意义的。

提示：如果列表选项很多，可以使用 <optgroup> 标签对相关选项进行组合
# 如果你有很多的选项组合, 你可以使用<optgroup> 标签能够很简单的将相关选项组合在一起
# <script> 标签用于定义客户端脚本，比如 JavaScript
# <select> 元素用来创建下拉列表。

<select> 元素中的 <option> 标签定义了列表中的可用选项
# <source> 标签为媒体元素（比如 <video> 和 <audio>）定义媒体资源
<source> 标签是 HTML5 中的新标签
# 被 <span> 元素包含的文本，您可以使用 CSS 对它定义样式，或者使用 JavaScript 对它进行操作
# type 属性指示 <style> 与 </style> 标签之间的内容。
值 "text/css" 指示内容是标准的 CSS。

# 使用 <u> 标签为文本添加下划线
把这个link放在head标签，用test.css文件里面的内容来定义CSS属性<link rel="stylesheet" href="test.css">

<link> 标签最常见的用途是链接样式表,例如上面的test.css就是自己定义的样式表文件
# HTML中几个常见的特殊字符  http://www.divcss5.com/html/h50257.shtml
&nbsp; 表示 空格

&amp; 表示  &

&lt;    表示   <

&gt;   表示   >

&quot;   表示 "

&qpos;  表示  '

textarea-------输入大段文本，例如产品介绍

autocomplete="on" 表单里这个属性表示自动保存之前输入的值，
placeholder="xiaozhan"   表示占位内容，可以随时输入新的内容